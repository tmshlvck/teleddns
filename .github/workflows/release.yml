name: Release

on:
  push:
    tags:
      - v[0-9]+.*

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  # Validate versions and create vendored tarball
  prepare:
    name: Prepare Release
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.version.outputs.version }}
      tarball: ${{ steps.version.outputs.tarball }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Check version alignment
        run: ./scripts/check-versions.sh

      - name: Extract version
        id: version
        run: |
          VERSION=$(grep '^version' Cargo.toml | head -1 | sed 's/.*"\(.*\)".*/\1/')
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "tarball=teleddns-${VERSION}.tar.gz" >> $GITHUB_OUTPUT
          echo "Version: $VERSION"

      - name: Verify tag matches version
        run: |
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          CARGO_VERSION="${{ steps.version.outputs.version }}"
          if [[ "$TAG_VERSION" != "$CARGO_VERSION" ]]; then
            echo "ERROR: Tag version ($TAG_VERSION) != Cargo.toml version ($CARGO_VERSION)"
            exit 1
          fi

      - name: Create vendored tarball
        run: ./scripts/create-vendor-tarball.sh .

      - name: Upload tarball artifact
        uses: actions/upload-artifact@v4
        with:
          name: vendored-tarball
          path: ${{ steps.version.outputs.tarball }}
          retention-days: 1

  # Publish to crates.io
  publish-crates:
    name: Publish to crates.io
    needs: prepare
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Publish to crates.io
        run: cargo publish --token ${{ secrets.CARGO_REGISTRY_TOKEN }}

  # Create GitHub Release with vendored tarball
  github-release:
    name: Create GitHub Release
    needs: prepare
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download tarball artifact
        uses: actions/download-artifact@v4
        with:
          name: vendored-tarball

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ needs.prepare.outputs.tarball }}
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_TOKEN }}

  # Build in Fedora COPR
  publish-copr:
    name: Build in COPR
    needs: [prepare, github-release]
    runs-on: ubuntu-latest
    steps:
      - name: Download tarball artifact
        uses: actions/download-artifact@v4
        with:
          name: vendored-tarball

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y python3-pip rpm
          pip3 install copr-cli

      - name: Configure copr-cli
        run: |
          mkdir -p ~/.config
          cat > ~/.config/copr << EOF
          [copr-cli]
          login = ${{ secrets.COPR_LOGIN }}
          username = tmshlvck
          token = ${{ secrets.COPR_TOKEN }}
          copr_url = https://copr.fedorainfracloud.org
          EOF

      - name: Build and submit SRPM
        run: |
          VERSION="${{ needs.prepare.outputs.version }}"
          # Use GitHub run number for RPM Release - auto-increments on each workflow run
          RPM_RELEASE="${{ github.run_number }}"

          # Extract tarball to get spec file
          tar xzf teleddns-${VERSION}.tar.gz

          # Update Release field in spec to use run number
          sed -i "s/^Release:.*/Release:        ${RPM_RELEASE}%{?dist}/" teleddns-${VERSION}/teleddns.spec

          # Set up rpmbuild directories
          mkdir -p ~/rpmbuild/{SOURCES,SPECS,SRPMS}
          cp teleddns-${VERSION}.tar.gz ~/rpmbuild/SOURCES/
          cp teleddns-${VERSION}/teleddns.spec ~/rpmbuild/SPECS/

          # Build SRPM
          rpmbuild -bs ~/rpmbuild/SPECS/teleddns.spec

          # Submit SRPM to COPR
          SRPM=$(ls ~/rpmbuild/SRPMS/*.src.rpm)
          copr-cli build tmshlvck/teleddns --nowait "$SRPM"

  # Publish to Launchpad PPA
  publish-ppa:
    name: Publish to Launchpad PPA
    needs: prepare
    runs-on: ubuntu-latest
    strategy:
      matrix:
        series: [noble, jammy, questing]
    steps:
      - name: Download tarball artifact
        uses: actions/download-artifact@v4
        with:
          name: vendored-tarball

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y devscripts debhelper gpg dput

      - name: Import GPG key
        run: |
          echo "${{ secrets.PPA_GPG_PRIVATE_KEY }}" | gpg --batch --import
          GPG_KEY_ID=$(gpg --list-secret-keys --keyid-format LONG | grep sec | head -1 | awk '{print $2}' | cut -d'/' -f2)
          echo "GPG_KEY_ID=$GPG_KEY_ID" >> $GITHUB_ENV

      - name: Build and upload source package
        env:
          DEBFULLNAME: "Tomas Hlavacek"
          DEBEMAIL: "tmshlvck@gmail.com"
        run: |
          VERSION="${{ needs.prepare.outputs.version }}"
          SERIES="${{ matrix.series }}"
          # Use GitHub run number for PPA revision - auto-increments on each workflow run
          PPA_REV="${{ github.run_number }}"

          # Extract tarball
          tar xzf teleddns-${VERSION}.tar.gz
          cd teleddns-${VERSION}

          # Update changelog for this series (version becomes X.Y.Z-1~ppaN~series1)
          dch --distribution ${SERIES} --local "~ppa${PPA_REV}~${SERIES}" "PPA release for ${SERIES}"

          # Create orig tarball (required for non-native packages)
          cp ../teleddns-${VERSION}.tar.gz ../teleddns_${VERSION}.orig.tar.gz

          # Build source package only (no binary build, skip dep check)
          debuild -S -sa -d -k${{ env.GPG_KEY_ID }}

          # Upload to PPA (files are in parent directory)
          dput ppa:tmshlvck/teleddns ../teleddns_${VERSION}-1~ppa${PPA_REV}~${SERIES}1_source.changes
